[project]
name = "Sketch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.0"
dependencies = [
    "opencv-python>=5.0.0.93",
    "pandas>=3.0.5",
]
